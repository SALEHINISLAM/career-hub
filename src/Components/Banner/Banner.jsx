import React from 'react';
import PropTypes from 'prop-types';

const Banner = props => {
    return (
        <div>
            <h2>Home page banner</h2>
        </div>
    );
};

Banner.propTypes = {
    
};

export default Banner;